apiVersion: 1

datasources:
  - name: Prometheus
    # The provisioned dashboard pins this uid, so it must not be left to
    # Grafana to generate.
    uid: PROM
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
